# v1.3 morning acceptance checklist (do this in order once the phone is back)

6 unpushed local commits (A, D, docs, B, docs, fix), all only reaching "build passes" — not one has run on a real device yet. **Verify before pushing, and before cutting the release.**

## 0. Install

```
. H:\ai_tool\jev-android\env.ps1; Set-Location H:\ai_tool\jev-android
adb install -r app\build\outputs\apk\debug\app-debug.apk
adb shell appops set com.jev.probe SYSTEM_ALERT_WINDOW allow
```
Then **on the phone, turn accessibility off and back on** (the screenshot capability was added, and HyperOS doesn't hot-reload meta-data). Check `adb logcat -s JEVASSIST` for `capture service connected` and `prefs migrated judgeKey.len=N` (N>0 = the old key migrated successfully).

## 1. A: configurable endpoints
- Open Settings → the three endpoint cards render correctly, and the judge card still has the old OpenRouter key.
- Tap "Test judge" and "Test reply" once each, check the latency and result; switch the reply endpoint to the DeepSeek official preset and back to OpenRouter (a preset must swap both the address and the model together).
- Open a conversation in WeChat or QQ and run one analysis to confirm the main pipeline still works.

## 2. D: knowledge base / context
- Settings → Analysis → tap the small "Self-check" link, it should return "passed."
- In "Knowledge base & contacts," create one always-on note containing a made-up fact (e.g. "I'm going to Chengdu next Wednesday"); set up a profile with an alias for some contact; turn on "record chat history."
- Analyze that contact: the panel should show "Knowledge N · History M," and the fact should show up in the reply candidates.
- After tapping "clear knowledge base & history," the counts should reset to zero.

## 3. B: OCR (the least certain part, explore first)
- In any app, long-press the overlay bubble → "Read screen once (OCR)." Check the panel: candidates appear = the screenshot was allowed; "internal error / system refused" appears = HyperOS refused the disguised service permission to screenshot → per the revision, stand up a separate, non-disguised, screenshot-only service (new task).
- Open a conversation in Feishu: the message body should be OCR'd and analyzed; check whether me/other is inverted (deciding "me" from the read-status container is an inference).
- In WeChat, check logcat to make sure it isn't taking a screenshot every second (should be deduplicated by signature).

## 4. C: wrap-up (only after verification passes)
- versionCode 4 / versionName 1.3, keep `targetSdk` at 35.
- `assembleRelease`, verify with apksigner, record the build size (expected around 27 MB).
- Replace v1.2 with `apk/jev-assistant-v1.3-release.apk`, update the README links and "known limitations" based on real-device results, then push:
  `env -u GITHUB_TOKEN git -c credential.https://github.com.helper= -c credential.helper=manager push origin main`

## Review reports (Grok 4.7)
`_reports/v13_plan_review_grok.md`, `v13_a_review_grok.md`, `v13_d_review_grok.md`, `v13_b_review_grok.md`; per-phase implementation reports at `_reports/v13_{a,d,b,fix}_report.md`. "Minor issues that can be left alone" from the reviews weren't addressed — deal with them if they show up on a real device.
